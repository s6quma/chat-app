<%= if controller_name != 'sessions' %>
  <%= link_to 'パスワードを忘れましたか？', new_session_path(resource_name), class: 'btn'%>
<% end %>

<%=if devise_mapping.registerable? && controller_name != 'registrations' %>
  <%= link_to 'サインアップ', new_registration_path(resource_name), class: 'btn'%>
<% end %>

